server:
  port: 8008

spring:
  application:
    name: pay-service-wechat
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
